{
    "name": "esim-bot-project",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "cd webapp && npm install --legacy-peer-deps && cd ../bot && npm install",
        "build": "cd webapp && npm run build"
    }
}